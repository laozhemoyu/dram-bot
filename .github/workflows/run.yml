name: TrendForce Monitor PRO

on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * *' # 每天北京时间 10:00 运行

jobs:
  run_monitor:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      # 🔥 解决乱码：安装文泉驿微米黑等中文字体
      - name: Install Chinese Fonts
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-wqy-microhei fonts-wqy-zenhei

      # 安装所有必要依赖
      - name: Install Dependencies
        run: |
          pip install selenium webdriver-manager requests matplotlib beautifulsoup4 openai

      # 🔥 运行脚本并传递环境变量
      - name: Execute Python Script
        env:
          # 必须确保你已在 GitHub Settings -> Secrets 中添加了以下四个变量
          DING_WEBHOOK: ${{ secrets.DING_WEBHOOK }}
          DING_SECRET: ${{ secrets.DING_SECRET }}
          AI_API_KEY: ${{ secrets.AI_API_KEY }}
          AI_BASE_URL: ${{ secrets.AI_BASE_URL }}
        run: python main.py
